name: Image

on:
  push:
    branches:
      - main
    paths:
      - "Dockerfile"
      - "nix/**"
      - ".github/workflows/image.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: docker/setup-buildx-action@master
      - uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@master
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/yifen9.li:latest
            ghcr.io/${{ github.repository_owner }}/yifen9.li:${{ github.sha }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository_owner }}/yifen9.li:cache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository_owner }}/yifen9.li:cache,mode=max
